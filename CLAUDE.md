# CLAUDE.md


## Project Overview

Johannes is a FastAPI backend application serving multiple integrated services: YNAB financial transaction management, OCR transcription via Google Gemini AI, and a CV portfolio with an interactive Rust/WebAssembly game. Deployed to both Azure AKS and a Raspberry Pi Kubernetes cluster.


## Your tasks: 

Your tasks is mostly to work on the frontend logic where i need help to build a beautiful web interface which fits the backend endpoints i will be building. The frontend in html files mounted as static files in static/ . Very rarely will i ask for help to debug or build logic in the fastapi backend
